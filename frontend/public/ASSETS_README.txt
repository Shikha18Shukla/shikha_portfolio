Place your resume PDF here as `resume.pdf` (referenced by the "View Resume" button
as `/resume.pdf`). Also drop a 1200x630 `og-image.png` here for social-share previews
— it's referenced in index.html's Open Graph tags.

Delete this file once both assets are in place.
